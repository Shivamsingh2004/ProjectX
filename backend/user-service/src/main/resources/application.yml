server:
  port: 8082
spring:
  application:
    name: user-service

supabase:
  jwks-uri: ${SUPABASE_JWKS_URI:https://placeholder.supabase.co/auth/v1/.well-known/jwks.json}
  issuer: ${SUPABASE_ISSUER:https://placeholder.supabase.co/auth/v1}
