import { Card } from "@/components/ui";

export default function SettingsPage() {
  return (
    <Card title="Settings">
      <p>Manage account security, sessions, and notification preferences.</p>
    </Card>
  );
}
