apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka
  namespace: dating-aggregator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka
  template:
    metadata:
      labels:
        app: kafka
    spec:
      containers:
        - name: kafka
          image: bitnami/kafka:3.7
          ports:
            - containerPort: 9092
---
apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: dating-aggregator
spec:
  selector:
    app: kafka
  ports:
    - port: 9092
      targetPort: 9092
